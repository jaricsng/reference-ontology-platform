# Capstone — Scale & Reflect
# Install into the repo venv (Python 3.12): pip install -r requirements.txt
# Spark requires JDK 17 or 21 (NOT 26); run.sh pins JAVA_HOME to openjdk@17.
pyspark>=3.5
pandas>=2.0
pyarrow>=15
duckdb>=1.0
